import React from 'react';
import ContentLayout from '../Layouts/Content';

export const ListPage = () => {
  return (
    <div>
      {'list results here'}
    </div>
  );
};

export default ContentLayout(ListPage);
